@model Domain.Entities.Player

@{
    ViewBag.Title = "Details";
}

<h2>Szczegóły - @Model.Name</h2>

<div>
    
	<hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.Label("Piłkarz: ")
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt>
            @Html.Label("Klub: ")
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Club.Name)
        </dd>

        <dt>
            @Html.Label("Pozycja:")
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Position.Name)
        </dd>

    </dl>
</div>
<p>
   <!-- @Html.ActionLink("Edit", "Edit", new { id = Model.PlayerId }) |-->
    @Html.ActionLink("Lista piłkarzy", "Index",null, new{@class="btn btn-success"})
</p>
